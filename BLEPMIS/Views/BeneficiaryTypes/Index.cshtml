@model IEnumerable<DAL.Models.Domain.MasterSetup.BeneficiaryType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/_Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BeneficiaryTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BeneficiaryTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BeneficiaryTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BeneficiaryTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BeneficiaryTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
